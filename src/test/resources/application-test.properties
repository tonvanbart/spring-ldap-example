# overrides for values provided in src/main/resources/application.properties

# test ldap url not used, test uses embedded server
ldap.url=ldap://localhost:636
spring.ldap.embedded.ldif=test-schema.ldif
spring.profiles.active=none